
  # Dispatcher Medical MVP prototype

  This is a code bundle for Dispatcher Medical MVP prototype. The original project is available at https://www.figma.com/design/izKTgQv3yWOH1diA5zbuaz/Dispatcher-Medical-MVP-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  